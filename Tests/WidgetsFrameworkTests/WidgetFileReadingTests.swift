//
//  WidgetFileReadingTests.swift
//  ApplicationTests
//
//  Created by Stanislav Matvichuck on 23.09.2022.
//

import XCTest

class WidgetFileReadingTests: XCTestCase {
//    private var sut: WidgetFileReader!

    override func setUp() {
        super.setUp()
//        sut = WidgetFileReader()
    }

    override func tearDown() {
        super.tearDown()
//        sut = nil
    }

    func testInit() {
//        XCTAssertNotNil(sut)
    }

    func testRead_readsPreviewFile() {
//        let decoder = PropertyListDecoder()
//
//        guard
//            let url = Bundle(for: WidgetFileReader.self).url(forResource: "WidgetPreview",
//                                                             withExtension: "plist")
//        else { fatalError("unable to find preview plist file") }
//        guard let fileContent = try? Data(contentsOf: url)
//        else { fatalError("unable to access preview file content") }
//        guard let decodedViewModel = try? decoder.decode(WidgetViewModel.self, from: fileContent)
//        else { fatalError("preview file decoding error") }
//
//        let previewCount = 2
//
//        XCTAssertEqual(decodedViewModel.count, previewCount)
    }
}
